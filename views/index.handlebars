<div class="leftburger">
				{{#each burgers}}
					{{#unless this.devoured}}
					{{this.burger_name}} </p>
							<span class="input-group-btn">
								<form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
									<input type="hidden" name="devoured" value="true">
									<button class="btn btn-default btn-lg" type="submit"> DEVOUR IT! </button>
								</form>
							</span>
						</div>
					</li>
					{{/unless}}
				{{/each}}
			</ul>
		</div>
	</div>

<div id="devour">
</div>

<div id="rightburger">
</div>

<form id="addburger" action="/create" method="POST">
	<textarea type="text" name="newBurger"></textarea>

	<button type="submit">Submit</button>
</form>

<h3>Enter a delicious burger then click the Submit button</h3>



	